project_name:
  type: str
  help: What is your project name? (e.g. my-awesome-project)
  default: my-project
project_slug:
  type: str
  help: What is your project slug? (e.g. my_awesome_project)
  default: my_project
